name: github actions
on: push
jobs:
  action-checkout: 
    runs-on: ubuntu-latest
    steps:
     - name: checkout the code
       uses: actions/checkout@v4
     - name: print index.html file
       run: cat index.html
