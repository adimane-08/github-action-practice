name: sample
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build step
        run: echo "a demo build
  
  test:
    runs-on: ubuntu-latest
    steps:
      - name: test phase
        run: echo "testing phase"
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy phase
        run: echo "deploying phase"
        
