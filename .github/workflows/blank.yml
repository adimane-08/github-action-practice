name: hello word
on: 
  push:
    branches:
      - main
      - feature/*
jobs:
  build: 
    runs-on: ubuntu-latest
    steps:
      - name: hello 
        run: |
          echo "welcome to github-actions"
 